<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
  
    <!-- Permission to access the internet -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">

        <activity
            android:name=".DictionaryActivity"
            android:exported="true">
          
        <intent-filter>
            <action android:name="android.intent.action.MAIN"/>

            <category android:name="android.intent.category.LAUNCHER"/>
        </intent-filter>
            </activity>

        <activity
            android:name=".deezerAppJoel.FavoriteSongsActivity"
            android:exported="false" />

        <activity
            android:name=".FavoriteActivity"
            android:exported="false" />
                  
        <activity
            android:name=".deezerAppJoel.SongListActivity"
            android:exported="false" />
      
        <activity
            android:name=".deezerAppJoel.ArtistListActivity"
            android:exported="false" />

        <activity 
            android:name=".deezerAppJoel.DeezerMainActivity"
            android:exported="false"/>

        <activity
            android:name=".MainActivityTing"
            android:exported="false" />

    </application>

</manifest>